View host nodes:
    Supply HTML for view mode
    Respond to event, if needed. Code is in the filter.
        After translation is finished


DB to CK:
    Supply HTML that includes widgety class.
    Supply server-side class for CK plugin.
    Callbacks for dialog
        Resty
    Hooks:
        After HTML preped
        Include extra client-side files.

CK to db:

